---
title: "EventKit 集成验收"
type: review
topic: system
workspace: "00-系统"
created: "2026-08-23 10:38:29"
modified: "2026-08-23 14:58:14"
tags: ["eventkit", "acceptance", "macos"]
source: agent
status: active
---

# EventKit 集成验收

## 验收边界

本报告记录仓库自动验证，以及用户在场授权后使用专用测试 Calendar 和 Reminder list 完成的九项真实 macOS App 验收。本次更新只写入已验证的有界结果；报告 Agent 没有再调用 EventKit，也没有删除或修改 Apple 条目。

报告不包含任何私人标题、备注、位置、URL、参与者或无关 Calendar/Reminder 数据。

## 自动验证

| Check | Result | Evidence |
|---|---|---|
| Daily Agent Node tests | pass | Task 9 修复后：54 pass / 0 fail |
| EventKit adapter Node tests | pass | `node --test 00-系统/Skills/eventkit/tests/*.test.mjs`：12 pass / 0 fail |
| EventKit Bridge Swift tests | pass | Task 9 修复后：50 pass / 0 fail |
| EventKit Bridge release build | pass | `bash 00-系统/Skills/eventkit/scripts/build-bridge.sh`：exit 0，release executable 与用途声明检查成功 |
| Subsystem audit | pass with known unrelated findings | `audit-subsystems --vault . --write-report`：64 OK / 0 warnings / 4 errors，与基线一致；4 个 error 仅来自 2 个既有 Inbox 剪藏 |
| Git whitespace check | pass | `git diff --check`：exit 0，无输出 |

## 真实 macOS App 验收

| Scenario | Result | Evidence |
|---|---|---|
| Calendar read | pass | `calendar.list [2026-08-23T00:00+08:00, 2026-08-24T00:00+08:00)` 匹配到前缀测试 identifier |
| Calendar create | pass | Bridge 创建了前缀测试 Event，用户在 Calendar.app 中看到并可编辑 |
| Apple → Pi edit | pass | 用户将测试 Event 移到本地时间 17:00–18:00；fresh fetch 返回 09:00–10:00Z |
| Pi → Apple edit | pass | Bridge update 成功，后续 get 返回更新后时间；在用户执行 App 编辑前，Calendar.app 已显示 Pi 设置的 16:00–17:00 |
| Reminder create | pass | Bridge 创建了前缀测试 Reminder，并出现在 Reminders.app |
| Apple completion | pass | completed predicate 返回 `completed: true` 和 `completionDate`；同步后本地 ID 变化暴露了 identifier churn 问题 |
| Pi completion | pass | Bridge complete 成功；用户随后在 Reminders.app 中观察到测试 Reminder 为已完成 |
| Reminder reopen | pass | Bridge reopen 成功；用户在后续手动检查 Reminders.app 时观察到测试 Reminder 为未完成；Task 9 后用过期本地 ID + `externalId` 执行 get/reopen/complete 也成功 |
| Recurrence span | pass | 前缀每日 all-day Event 返回 `recurring: true`；缺少 span 的 update 返回 `RECURRING_EVENT_REQUIRES_SPAN`；`thisEvent` 仅更新 2026-08-23 occurrence 的 notes，2026-08-24 仍 recurring 且未改变 |

## Task 9 identifier churn 修复复验

- Apple completion 后 Reminder 的本地 identifier 发生变化，使旧本地 ID 失效。
- Task 9 在 `f6b08f4` 与 `989ab07` 中加入本地 ID 优先、`externalId` 按需 fallback，并拒绝不完整 locator 参数。
- 修复后真实 get/reopen/complete 使用过期本地 ID + `externalId` 均成功。
- 修复后自动验证总计：Swift 50/50、Daily Agent 54/54、EventKit adapter 12/12。

## 清理状态

专用测试条目的清理尚未执行，等待用户明确确认。未经确认不得删除任何 Calendar 或 Reminder 条目。

## 当前结论

`PASS_WITH_CLEANUP_PENDING`：九项真实 macOS App 验收均通过，Task 9 修复后的 identifier fallback 已完成真实复验；专用测试条目仍等待用户明确确认后清理。
